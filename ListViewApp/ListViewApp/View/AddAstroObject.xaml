<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ListViewApp.View.AddAstroObject"
             BackgroundColor="Black">
    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="EntryName" Placeholder="Název" HorizontalTextAlignment="Center" Text="{Binding Name}" PlaceholderColor="White" TextColor="White"/>
            <Entry x:Name="EntryType" Placeholder="Typ" HorizontalTextAlignment="Center" Text="{Binding Type}" PlaceholderColor="White" TextColor="White" />
            <Entry x:Name="EntryDescription" Placeholder="Popis" HorizontalTextAlignment="Center" Text="{Binding Description}" PlaceholderColor="White" TextColor="White" />
            <Button Text="Uložit" HorizontalOptions="Center" VerticalOptions="End" Clicked="Save_Button_Clicked" BackgroundColor="White" TextColor="Black"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>